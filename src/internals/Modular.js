export default {

    /**
     * @method setup
     * @param name {String}
     * @param dependencies {Array}
     * @return {Object}
     */
    setup: function setup(name, dependencies) {

        return {
            name: name,
            dependencies: dependencies
        }

    }

}